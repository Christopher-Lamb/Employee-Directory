import React, { Component } from "react";
// import API from "../utils/API";

class Home extends Component {
  state = {
    image: "",
    match: false,
    matchCount: 0,
  };

  render() {
    return <div></div>;
  }
}

export default Home;
